# FIFO Five：交给 Codex 的开发任务书

请在当前工作目录开发一个实际可运行的网页游戏，暂名 **FIFO Five / 时序连子棋**。请先检查现有文件与 AGENTS.md；已有项目优先沿用结构，不覆盖用户未提交的工作。空目录则按下列技术路线初始化。不要只输出方案或代码片段：按阶段实现、执行测试和构建、修复问题，最后给出启动方法与真实验证结果。

## 1. 目标与范围

第一版做成不需要账号、不需要后端服务、不需要模型训练、不调用付费 API 的网页游戏。玩家可以与电脑对战，也可以同机双人对战；通过设置比较不同棋盘、胜利连线长度和棋子数量上限。默认规则是 6×6、五连获胜、每人最多保留六枚棋。

扩展性应体现在配置、纯规则引擎、统一 AI 接口和可重放的棋谱上，不是第一版就实现所有想得到的玩法。暂不做联机、排行榜、账号、商业化、神经网络、GPU 推理、MCTS、重力落子或任意脚本插件。允许以后增加其他搜索算法，但不要为这些未实现的功能设置空按钮。

默认参数只是待试玩的候选，未证明理论公平，也未证明全球首创。不要在代码、README 或界面使用前面对话中未经实际运行验证的胜率、对局数、棋力、强求解结论或性能数字。

## 2. 技术路线

空项目优先使用 TypeScript 严格模式、React、Vite、Vitest，采用 npm 并提交锁文件。选择彼此兼容的稳定依赖，实际验证安装和构建；不要猜版本号。样式使用简单 CSS 即可，避免仅为棋盘引入大型 UI 库。

规则引擎与 AI 使用纯 TypeScript，不依赖 React、DOM、网络或 localStorage；同一份代码供浏览器与 Node 实验脚本复用。浏览器 AI 放在 Web Worker 中。只建立必要模块，不搭建复杂 monorepo 或通用插件框架。

## 3. 规则的唯一正式定义

### 3.1 默认模式

棋盘为 6×6 的格子，双方轮流在一个当前为空的格子放置自己的棋，先手使用 X，后手使用 O。没有重力，也没有禁手。横、竖、两种斜向任一方向连续不少于五枚同色棋即获胜；没有“必须恰好五枚”的限制。

双方各自维护一个从最老到最新的棋子队列，默认上限 K=6。未到上限时不删除。已到上限后，每次必须先选择当前空格、放入新棋，再从本方队列头部删除最老的一枚，最后判断胜负。这不是全局回合倒计时；对方落子不会触发本方棋子删除。

一次行动按以下原子顺序处理：

1. 检查游戏未结束、坐标合法且落子前该格为空。
2. 放入当前玩家新棋，追加到其队列末尾。
3. 队列超过 K 时删除队首棋子；永久棋模式不删除。
4. 在删除后的稳定棋盘检查当前玩家是否获胜。只有稳定结果算数，中间暂时出现的连线不算胜利。
5. 未获胜则切换行动方，并按下述规则检查和棋。

默认不能落在自己即将消失的棋所在格：选择位置时它仍被占据。不要偷偷改成“先删旧棋再选位置”，也不要允许原地续命。

在这一无重力、只删除本方棋、终局立即停止的规则下，合法进行中的一次落子不会新建对手的连线。不要添加“双方同时成线时谁优先”等无关规则；如导入数据含非法双胜局面，应拒绝导入。

### 3.2 参数化

第一版配置至少包括 boardSize、winLength、retention 和可选的 matchPlyLimit，并有明确的 rulesVersion。用带标签的结构表达 retention：`{ kind: "fifo", maxStones: K }` 或 `{ kind: "permanent" }`，不要用一个含义不清的零或负数代表无限。

用户界面支持正方形棋盘边长 3～9，连线长度 3～boardSize。FIFO 的 K 必须是整数且满足 winLength ≤ K ≤ boardSize²；非法设置给出中文原因。K 很大时允许棋盘被填满，按无合法着法和棋处理，不悄悄修改落子顺序。不同参数不是平衡性保证。引擎不要散落写死的 6、5、6。

第一版预设：默认 6×6 / 连5 / K6；实验用 5×5 / 连4 / K4、K5、K6；6×6 / 连5 / K5、K7；7×7 / 连5 / K6；经典 3×3 / 连3 / 永久棋。用同一规则配置生成预设，不复制引擎。设置变更只在新对局生效；正在下的对局须明确重新开始。

允许将来增加其他棋盘或删除策略，但现在只实现 FIFO 与永久棋这两种；新增策略必须一起定义合法落子、胜负时机和测试，不能仅换一个名称。

### 3.3 三次重复、满盘与步数限制

同一“规则局面”第三次出现自动和棋。局面键包括冻结的规则配置签名、轮到谁，以及双方棋子位置的相对年龄顺序。不能只比较 X/O 占位，也不能把绝对手数、时间戳、递增棋子 ID 放入重复键，否则真正的循环将识别不出来。初始局面记一次，之后只统计完整行动结束后的稳定局面；已经获胜的行动优先按胜利处理。

游戏历史另存各局面出现次数。没有合法落子时和棋。正常对局默认无步数上限，只使用上述终局规则；可选 matchPlyLimit 必须是公开的比赛规则，并显示对应终局原因。默认不启用。

批量实验可设置与游戏规则分离的运行截止手数，到达后记录 `cutoff`，不能当成已证明和棋，也不能混入 repetition draw。UI、存档与结果统计应区分获胜、三次重复、无合法着法、规则步数上限、实验截断。

## 4. 数据与模块边界

建议按 core、ai、workers、ui、experiments、tests 划分目录，文件数量以实际需要为准。core 至少提供配置校验、初始局面、合法着法、落子推进、胜负检测、局面键、棋谱重放等 API。合法落子函数返回新状态，不修改输入；返回可用于动画的新增棋、被移除棋和获胜连线事件，UI 不自行重算或修改规则。

有序队列采用队首最老、队尾最新。棋盘占位可以由队列派生或作为受校验缓存，但不要让两份可变数据互相失配。基础实现可使用数组与预计算连线。若优化为位棋盘，必须选择能表示全部格子的方案并测试边界，不允许把 36～81 格塞进一个 32 位位运算掩码。

区分 positionKey 与完整的对局/搜索上下文：前者用于识别重复局面，后者还包含历史重复计数、规则步数上限剩余量等。棋谱保存 schemaVersion、完整规则配置、从开局开始的落子序列，以及可选的玩家/AI 元数据。导入后通过规则引擎逐手重放以重建棋龄和重复计数，不直接信任文件里的 winner 或棋盘截图。

## 5. 不训练的电脑玩家

### 5.1 统一接口与基线

提供统一 chooseMove 接口，接收局面、完整规则/重复上下文及搜索预算，返回合法落点和诊断信息。预算包含时间、节点数与随机种子；结果至少记录实际完成深度、节点数、耗时、是否预算耗尽及候选主变化。没有搜索完成也须有合法后备着法；无合法着法或终局返回无着法，不抛出导致页面崩溃的异常。

实现 seeded random 作为实验基线，另实现战术 AI，再实现搜索 AI。正常和困难难度用同一搜索器的不同预算配置，不另写互不一致的规则。难度预算是产品配置，不承诺固定达到六层或八层，也不宣称不可战胜。

### 5.2 战术检查必须懂 FIFO

先枚举当前全部合法着法，发现稳定局面的一手胜就选取。没有立即胜时，对每个候选推进完整本方行动，再检查对手下一手是否存在合法的一手胜：存在安全回复时不得选择明显送给对手一手胜的着法；无安全回复时如实处理，不宣称已经防住。

不能通过把行动方字段随便改成对手、保持其他状态不变来假装预测对手。必须走完整真实状态转移，处理本方删除之后开放的新空格以及对方下一步也会删除的老棋。不能把含即将过期棋的“四连”直接认作必胜，也不能一律通过占据对手目标点防守。

### 5.3 搜索

使用 Negamax/Minimax、Alpha-Beta、迭代加深与着法排序。评价依据连线机会、真实可完成威胁、棋龄、关键阻挡的即将消失等，评分是启发式值，不是经过校准的胜率。保证终局分值优先级高于任何启发式值；未完整搜索的结果不得标记为数学证明。

每层传递并回溯恢复三次重复所需计数。置换表是可选优化：第一版可以只缓存最佳着法用于排序。若缓存分值或上下界，键必须覆盖影响终局的历史重复上下文及剩余规则步数预算；只按棋盘和年龄复用“胜/负/和”分值是不合格的。搜索深度、上下界类型、规则签名也须正确处理。

精确的一手胜/一手防守检查覆盖全部合法点。不要默认只考虑已有棋附近的落点。若困难模式以后采用候选点裁剪或选择性延伸，必须标注为启发式，且给出节点/深度界限；不要把被裁掉的防守分支当作已经证明不存在。

### 5.4 Worker 生命周期

UI 始终可响应。搜索在 Worker 运行，重开、悔棋、导入棋谱、切换规则或离开页面时取消旧任务。每次请求携带对局代号和请求 ID，忽略迟到的旧响应；必要时终止并重建 Worker，不能依赖同步计算过程中无法及时处理的普通取消消息。

人机模式允许玩家执先/执后。AI 开局、AI 思考中的重开与悔棋都要测试。思考信息显示已完成深度和节点数即可，不伪造推理解释或胜率。

## 6. 可玩界面

默认中文，清晰适配桌面与手机。棋盘是可点击格子，X/O 或形状区分双方，不只依赖颜色。显示当前行动方、手数、规则摘要、双方当前棋子数量、AI 思考与终局原因。支持键盘操作、可辨识焦点和合适触控尺寸。

FIFO 模式用相对编号 1～当前数量显示队列顺序，1 是最老的，不是绝对第几手。只有队列达到 K 时才将最老棋标为“本方下次落子后消失”；还未到 K 时不要错误提示下一步就消失。永久棋模式隐藏到期提示。年龄显示可切换，但提示的语义必须准确。

放入新棋与旧棋消失可以有短动画，但动画不参与判定逻辑。高亮最后落子与最终获胜连线，尊重减少动态效果的设置。

第一版至少有新游戏、人机/同机双人、先后手选择、三档难度、预设/自定义规则，以及规则说明。再加入悔棋、JSON 棋谱导入导出和只读回放：同机悔一手；人机回到玩家上一次决策前，通常撤销一对落子，但 AI 尚未落子或玩家刚获胜时不能机械撤两手。没有历史时禁用。

导入文件须校验类型、配置、坐标、完整合法历史与版本，并设置合理体积/步数限制，出错展示可理解的中文信息。不要执行导入文件中的内容，也不要悄悄接受终局之后的额外落子。

## 7. 实验工具：共享引擎，不先假设结论

第一版可玩后，提供最小 Node CLI，使两个 AI 在指定参数、种子和预算下对弈，可用 JSON 配置批量运行候选。交换双方先后手，记录每局种子、算法版本、预算、规则、棋谱、结果、终局手数、实际删除次数与首次删除手数。

汇总分别报告先手/后手获胜、重复和棋、无合法着法和棋、规则步数上限、实验 cutoff；说明对局长度的统计口径，并单独统计真实胜负局。可以统计“产生胜负前是否发生过删除”，永久棋模式该项记不适用。不要从随机 AI 或弱 AI 自博弈推导理论公平，更不要从没有找到杀棋推导和棋。

确定性测试和可复现实验使用固定节点预算与种子。真人对战可用墙钟时间预算；报告应说明墙钟截止会随设备改变搜索量和棋谱。对完全相同的确定性对局重复多次不视为额外独立证据。

只运行合理的小规模冒烟实验来验证脚本，保留真实输出。没有执行的大批量实验写明未执行，不填入看起来合理的数字。

## 8. 测试与验收

详见 docs/ACCEPTANCE.md 和 fixtures/rule-cases.json。先完成规则测试，再接入 UI；没有浏览器能力时应报告未完成浏览器验证，不能用规则单测冒充完整游戏验收。

至少提供 npm run dev、npm run build、npm run typecheck、npm run lint、npm test，并让自动化测试命令在 CI 中执行后退出，而不是挂在监听模式。端到端测试优先用 Playwright，确实无法运行则说明障碍。提供适配 Windows 终端的启动说明。静态部署说明可面向 GitHub Pages，并考虑仓库子路径；此次不要未经授权推送、发布或更改用户线上资源。

## 9. 执行顺序和交付

先用一段简短计划列出模块和主要风险，随后直接实现，不等用户逐项批准。遇到小歧义采用不改变核心规则的保守选择并记入文档；不要自行改变先落后删、胜负时机和重复定义。

P0：规则引擎、参数校验、规则测试、战术与搜索 AI、Worker、基本可玩的网页及配置切换。优先交付从启动到人机下一盘的完整闭环。

P1：人机悔棋、棋谱导入导出/回放、更完整 AI 回归测试、最小批量实验脚本、构建与部署说明。在 P0 可运行的基础上完成，不为了视觉装饰牺牲这些行为的正确性。

保持短的 AGENTS.md 持久约束，详细规则留在本文件，真实进展记入 docs/PROGRESS.md。README 应说明玩法、启动、测试、AI 原理、参数化方式、已知限制及实验如何复现；不自称首创、已强求解、必然平衡或拥有未经测量的性能。

最后汇报实际完成的功能、真实执行的命令与结果、未完成或未验证项目，以及启动方式。不要将未测试的功能说成通过验收。不要止步于计划；按此任务书开发实际可运行的版本。
