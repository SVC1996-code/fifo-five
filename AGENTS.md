# AGENTS.md

本仓库开发 FIFO Five：可配置的无重力时序连子棋。先阅读 CODEX_TASK.md 与 docs/ACCEPTANCE.md，按阶段交付可玩的网页，不只输出方案。

## 不可改变的默认规则

6×6、连至少五枚获胜、每人 FIFO 保留六枚。新棋必须放在行动前为空的格子；先放新棋，再删除本方最老棋，最后判胜。不能在即将消失的原格直接续命。

完整重复局面包含规则签名、行动方及双方相对年龄队列；排除时间戳、绝对手数和递增 ID。同局面第三次出现和棋。重复计数属于历史上下文，不是单看棋盘就能推导。实验手数截止与规则和棋须分开。

## 实现约束

规则、AI、UI 分层；core 不依赖 DOM/React。浏览器与 Node 实验复用同一引擎。使用严格 TypeScript，参数不得散落写死。浏览器搜索放 Worker，取消及过期响应必须安全处理。

AI 不训练、不调用付费 API，先做真实 FIFO 状态转移下的一手胜/防守，再做限预算 Alpha-Beta。TT 首版可仅用于着法排序；复用分值必须考虑重复历史等终局上下文。截断搜索不是证明，启发式分不是胜率。

保留用户已有代码与未提交改动；不自行推送、发布或覆盖线上服务。详细设计放任务书，实际进展放 docs/PROGRESS.md，保持本文件简短。

## 验证与诚实报告

实现并执行 npm test、npm run typecheck、npm run lint、npm run build。测试不应默认永久监听。浏览器测试能运行则运行；受环境限制时明确报告未验证。

fixtures/ 包含独立检查过的规则样例，不代表网页或 AI 已完成。不得沿用先前聊天里未经执行验证的模拟数字，不得伪造测试、胜率、运行日志或强求解结论。
